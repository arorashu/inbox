{
  "name": "inbox",
  "version": "0.1.0",
  "type": "module",
  "description": "Personal inbox — save, read, search. Agent-first.",
  "bin": {
    "inbox": "./bin/inbox.ts"
  },
  "scripts": {
    "inbox": "bun run bin/inbox.ts",
    "serve": "bun run src/server.ts",
    "dev": "bun --watch bin/inbox.ts"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "defuddle": "^0.18.1",
    "hono": "^4.12.23",
    "linkedom": "^0.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
